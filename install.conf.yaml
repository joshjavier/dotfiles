- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.vimrc: vim/init.vim
    ~/.config/nvim/init.vim: vim/init.vim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
